version: '3.8'

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_URL: ${DB_URL}
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data

# COMMANDS: docker-compose up -d, docker-compose down